<!-- edit.html.erb -->
<div class="container mx-auto px-4">
  <div class="text-center mb-6">
    <h1 class="text-2xl font-bold text-gray-800">Editing Item</h1>
  </div>

  <%= render "form", item: @item %>

  <div class="mt-4 flex space-x-2">
    <%= link_to "Show this item", @item, class: "px-4 py-2 text-white bg-green-600 rounded hover:bg-green-700" %>
    <%= link_to "Back to items", items_path, class: "px-4 py-2 text-white bg-gray-600 rounded hover:bg-gray-700" %>
  </div>
</div>